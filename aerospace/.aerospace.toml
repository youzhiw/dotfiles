# Reference: https://github.com/i3/i3/blob/next/etc/config
# All possible modifiers: cmd, alt, ctrl, shift

# i3 doesn't have "normalizations" feature that why we disable them here.
# But the feature is very helpful.
# Normalizations eliminate all sorts of weird tree configurations that don't make sense.
# Give normalizations a chance and enable them back.
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# start at login
start-at-login = true

after-startup-command = []

# Mouse follows focus when focused monitor changes
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

exec-on-workspace-change = ['/bin/bash', '-c',
    'sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE PREV_WORKSPACE=$AEROSPACE_PREV_WORKSPACE'
]

[mode.main.binding]
# See: https://nikitabobko.github.io/AeroSpace/goodies#open-a-new-window-with-applescript
#alt-enter = '''exec-and-forget osascript -e '
#tell application "/opt/homebrew/bin/kitty"
#    activate
#end tell'
#'''

alt-slash = 'layout tiles horizontal vertical'
alt-comma = 'layout accordion horizontal vertical'
#alt-enter = 'exec-and-forget /opt/homebrew/bin/alacritty msg create-window'
alt-enter = 'exec-and-forget /opt/homebrew/bin/alacritty msg create-window'
ctrl-alt-f = 'exec-and-forget /Applications/Firefox.app/Contents/MacOS/firefox -p personal'
ctrl-alt-g = 'exec-and-forget /Applications/Firefox.app/Contents/MacOS/firefox -p Columbia'
#exec-and-forget open -a /Applications/WezTerm.app

# i3 wraps focus by default
alt-h =         'focus --boundaries-action wrap-around-the-workspace left'
alt-j =         'focus --boundaries-action wrap-around-the-workspace down'
ctrl-alt-j =         'focus-monitor down'
ctrl-alt-k =         'focus-monitor up'
ctrl-alt-h =         'focus-monitor left'
ctrl-alt-l =         'focus-monitor right'
alt-k =         'focus --boundaries-action wrap-around-the-workspace up'
alt-l =         'focus --boundaries-action wrap-around-the-workspace right'

alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# Consider using 'join-with' command as a 'split' replacement if you want to enable normalizations
#alt-h = 'split horizontal'
alt-v = 'join-with left'
alt-shift-v = 'join-with down'

alt-f = 'fullscreen'

alt-s = 'layout v_accordion' # 'layout stacking' in i3
alt-w = 'layout h_accordion' # 'layout tabbed' in i3
alt-e = 'layout tiles horizontal vertical' # 'layout toggle split' in i3

alt-shift-space = 'layout floating tiling' # 'floating toggle' in i3

# Not supported, because this command is redundant in AeroSpace mental model.
# See: https://nikitabobko.github.io/AeroSpace/guide#floating-windows
#alt-space = 'focus toggle_tiling_floating'

# `focus parent`/`focus child` are not yet supported, and it's not clear whether they
# should be supported at all https://github.com/nikitabobko/AeroSpace/issues/5
# alt-a = 'focus parent'

alt-1 = 'workspace 1'
alt-2 = 'workspace 2'
alt-3 = 'workspace 3'
alt-4 = 'workspace 4'
alt-5 = 'workspace 5'
alt-6 = 'workspace 6'
alt-7 = 'workspace 7'
alt-8 = 'workspace 8'
alt-9 = 'workspace 9'
alt-0 = 'workspace 10'
alt-n = 'workspace N'
alt-m = 'workspace M'
alt-y = 'workspace Y'
alt-u = 'workspace U'
alt-i = 'workspace I'
alt-o = 'workspace O'
alt-p = 'workspace P'
alt-z = 'workspace Z'
alt-x = 'workspace X'
alt-b = 'workspace B'

alt-shift-1 = 'move-node-to-workspace 1 --focus-follows-window'
alt-shift-2 = 'move-node-to-workspace 2 --focus-follows-window'
alt-shift-3 = 'move-node-to-workspace 3 --focus-follows-window'
alt-shift-4 = 'move-node-to-workspace 4 --focus-follows-window'
alt-shift-5 = 'move-node-to-workspace 5 --focus-follows-window'
alt-shift-6 = 'move-node-to-workspace 6 --focus-follows-window'
alt-shift-7 = 'move-node-to-workspace 7 --focus-follows-window'
alt-shift-8 = 'move-node-to-workspace 8 --focus-follows-window'
alt-shift-9 = 'move-node-to-workspace 9 --focus-follows-window'
alt-shift-0 = 'move-node-to-workspace 10 --focus-follows-window'
alt-shift-n = 'move-node-to-workspace N --focus-follows-window'
alt-shift-m = 'move-node-to-workspace M --focus-follows-window'
alt-shift-y = 'move-node-to-workspace Y --focus-follows-window'
alt-shift-u = 'move-node-to-workspace U --focus-follows-window'
alt-shift-i = 'move-node-to-workspace I --focus-follows-window'
alt-shift-o = 'move-node-to-workspace O --focus-follows-window'
alt-shift-p = 'move-node-to-workspace P --focus-follows-window'
alt-shift-z = 'move-node-to-workspace Z --focus-follows-window'
alt-shift-x = 'move-node-to-workspace X --focus-follows-window'
alt-shift-b = 'move-node-to-workspace B --focus-follows-window'

alt-shift-r = 'reload-config'

alt-r = 'mode resize'

[mode.resize.binding]
h = 'resize width -50'
j = 'resize height +50'
k = 'resize height -50'
l = 'resize width +50'
enter = 'mode main'
esc = 'mode main'

[gaps]
outer.top = [{ monitor."Dell" = 50 }, 15]
inner.horizontal = 15
inner.vertical = 15
outer.left = 15
outer.bottom = 15
#outer.top = 50
outer.right = 15

[workspace-to-monitor-force-assignment]
1 = ['secondary', 'main']
2 = ['secondary', 'main']
3 = ['secondary', 'main']
4 = ['secondary', 'main']
5 = ['secondary', 'main']
6 = ['s2719','main', 'secondary']
7 = ['s2719','main', 'secondary']
8 = ['s2719','main', 'secondary']
9 = ['s2719','main', 'secondary']
10 = ['s2719','main', 'secondary']
N = ['dell','main', 'secondary']
M = ['dell','main', 'secondary']
Y = ['s2719','main', 'secondary']
U = ['s2719','main', 'secondary']
I = ['s2719','main', 'secondary']
O = ['s2719','main', 'secondary']
P = ['s2719','main', 'secondary']
Z = ['dell','main', 'secondary']
X = ['dell','main', 'secondary']
C = ['dell','main', 'secondary']
B = ['dell','main', 'secondary']

[[on-window-detected]]
    if.app-id = 'com.cisco.secureclient.gui'
    run = ['layout floating', 'move-node-to-workspace 4']
[[on-window-detected]]
    if.app-id = 'com.tinyspeck.slackmacgap'
    run = ['move-node-to-workspace 1']
[[on-window-detected]]
    if.app-id = 'md.obisidian'
    run = ['move-node-to-workspace 2']
[[on-window-detected]]
    if.app-id = 'com.microsoft.Outlook'
    run = ['move-node-to-workspace O']

